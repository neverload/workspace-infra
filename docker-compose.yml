name: workspace-infra

services:
  caddy:
    image: caddy:2
    container_name: caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    env_file:
      - .env
    volumes:
      - ./infra/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    extra_hosts:
      - "host.docker.internal:host-gateway"

  mongodb:
    image: mongo:7
    container_name: mongo
    environment:
      MONGO_INITDB_DATABASE: workspace_infra
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    restart: unless-stopped
    cpus: 2.0
    mem_limit: 1G
    networks:
      - dev-network

  dev:
    build: ./dev
    container_name: dev
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "2201:22"
      # Bind all host interfaces so Tailscale / LAN reaches container services (omit 127.0.0.1: prefix).
      - "${NEXTGIRL_WEB_PORT}:${NEXTGIRL_WEB_PORT}"
      - "${NEXTGIRL_API_PORT}:${NEXTGIRL_API_PORT}"
      - "${NEXTGIRL_WS_PORT}:${NEXTGIRL_WS_PORT}"
      - "${INTELINK_WEB_PORT}:${INTELINK_WEB_PORT}"
      - "${INTELINK_SERVER_PORT}:${INTELINK_SERVER_PORT}"
      - "${FUTURIST_WEB_PORT}:${FUTURIST_WEB_PORT}"
      - "${FUTURIST_SERVER_PORT}:${FUTURIST_SERVER_PORT}"
    volumes:
      - /home/admin/shared:/home/admin/shared
      - dev-data:/home/admin/.local
    networks:
      - dev-network

  # App ports intentionally only on `dev`: `dev`+`prod` share the same .env ports; duplicate publish would fail on compose up.
  prod:
    build: ./prod
    container_name: prod
    restart: unless-stopped
    ports:
      - "2202:22"
    volumes:
      - /home/admin/shared:/home/admin/shared
      - prod-data:/home/admin/.local
    runtime: nvidia
    networks:
      - dev-network

volumes:
  caddy_data:
    name: workspace_infra_caddy_data
  caddy_config:
    name: workspace_infra_caddy_config
  mongodb_data:
    name: workspace_infra_mongodb_data
  dev-data:
    name: workspace_infra_dev_data
  prod-data:
    name: workspace_infra_prod_data

networks:
  dev-network:
    driver: bridge
